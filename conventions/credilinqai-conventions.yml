generated: '2026-07-18'
method: searched
source: openapi/credilinqai-openapi.yml
docs: https://docs.credilinq.ai/docs/platform-guidelines.md
authentication:
  style: bearer-jwt
  model: oauth2-client-credentials (Auth0)
  header: Authorization Bearer <access_token>
  token_endpoint: /v1/auth/generate-token
  ref: authentication/credilinqai-authentication.yml
versioning:
  scheme: uri-path
  versions: [v1, v2]
  note: KYC customer/director create/update are on v2; the rest of the surface is on v1.
localization:
  header: accept-language
  supported: [en-us, in]
  default: en-us
  note: Most operations accept an accept-language header to localize responses.
error_envelope:
  format: custom-envelope
  fields: [statusCode, error, errorCode, message, success]
  ref: errors/credilinqai-problem-types.yml
idempotency:
  supported: false
  note: No idempotency-key header or parameter is documented in the OpenAPI or docs.
pagination:
  supported: unknown
  note: No cursor/offset pagination parameters are declared in the published spec.
webhooks:
  supported: true
  signing: HMAC SHA-256 over "<timestamp>.<body>" using the client_secret as the signing secret
  ref: asyncapi/credilinqai-webhooks.yml
environments:
  ref: sandbox/credilinqai-sandbox.yml
  hosts:
    sandbox: https://sandbox-api.credilinq.ai
    staging: https://stage-api.credilinq.ai
    production: https://api.credilinq.ai
